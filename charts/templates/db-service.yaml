apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-{{ .Values.db.name }}"
  labels:
    app: {{ .Values.db.name }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.db.port }}
    targetPort: {{ .Values.db.port }}
  selector:
    app: {{ .Values.db.name }}
